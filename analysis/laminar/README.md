# analysis/laminar/

Depth-resolved CSD / LFP aligned to alpha troughs.

| Script | Content |
| --- | --- |
| `alpha_csd_layers.py` | CSD across layers, aligned to the alpha trough |
| `laminar_aligned.py` | LFP/CSD aligned to both alpha and gamma troughs |
